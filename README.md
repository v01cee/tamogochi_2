# Tamogochi Bot

Telegram бот на Python с использованием SQLAlchemy 2.0, Alembic, PostgreSQL, Redis и aiogram.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка окружения

Скопируйте `.env.example` в `.env` и заполните переменные:

```bash
cp .env.example .env
```

Обязательно укажите все переменные из `.env.example`, особенно:
- `BOT_TOKEN` - токен вашего Telegram бота
- `POSTGRES_HOST`, `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB` - настройки PostgreSQL
- `SECRET_KEY` - секретный ключ Django
- `ROBOKASSA_SHOP_ID`, `ROBOKASSA_PASSWORD1`, `ROBOKASSA_PASSWORD2` - настройки Robokassa
- `CLOUDRU_IAM_KEY`, `CLOUDRU_IAM_SECRET` - ключи Cloud.ru API
- `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY` - ключи AWS S3
- И другие переменные из `env.example`

### 3. Запуск с Docker

```bash
docker-compose up -d
```

### 4. Запуск без Docker

1. Убедитесь, что PostgreSQL и Redis запущены
2. Выполните миграции:
```bash
alembic upgrade head
```
3. Запустите бота:
```bash
python bot.py
```

## 🗂 Django админка

После применения Alembic-миграций в корне проекта можно поднять административную панель:

```bash
cd admin_panel
python manage.py migrate         # применит лишь системные таблицы Django
python manage.py createsuperuser
python manage.py runserver 0.0.0.0:8042
```

По адресу `http://localhost:8042/admin/` доступны разделы:
- `Пользователи Telegram` — карточки пользователей, собранных ботом;
- `Результаты опроса` — стартовые оценки (с диаграммами в боте), привязанные к пользователям.

## 📁 Структура проекта

```
project/
├── alembic/              # Миграции базы данных
├── core/                 # Общая логика
│   ├── config.py        # Конфигурация
│   ├── keyboards.py     # Класс для клавиатур
│   └── texts.py         # Тексты и функции работы с ними
├── database/             # Работа с БД
│   ├── base.py          # Базовый класс моделей
│   ├── repository.py    # Базовый репозиторий
│   └── session.py       # Сессии SQLAlchemy
├── handlers/            # Обработчики бота
│   ├── start.py         # Команды /start, /help
│   └── callbacks/       # Разделённые по темам callback-хендлеры
├── models/              # Модели SQLAlchemy
│   ├── user.py          # Модель пользователя
│   └── quiz_result.py   # Модель результатов опроса
├── repositories/        # Репозитории
│   ├── user_repository.py
│   └── quiz_result_repository.py
├── bot.py              # Точка входа
├── admin_panel/        # Django-админка для просмотра данных
├── docker-compose.yml  # Docker конфигурация
├── Dockerfile          # Образ приложения
└── requirements.txt    # Зависимости
```

## 🔧 Разработка

### Создание миграций

```bash
alembic revision --autogenerate -m "Описание миграции"
alembic upgrade head
```

### Архитектура

- **SOLID принципы** - разделение ответственности
- **Repository pattern** - работа с БД через репозитории
- **Dependency Injection** - использование сессий через генераторы

## 📝 Переменные окружения

Все переменные окружения описаны в файле `env.example`. Основные:

**Обязательные:**
- `BOT_TOKEN` - Токен Telegram бота
- `POSTGRES_HOST`, `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB` - настройки PostgreSQL
- `SECRET_KEY` - Секретный ключ Django
- `ROBOKASSA_SHOP_ID`, `ROBOKASSA_PASSWORD1`, `ROBOKASSA_PASSWORD2` - настройки Robokassa
- `CLOUDRU_IAM_KEY`, `CLOUDRU_IAM_SECRET` - ключи Cloud.ru API для Whisper и Qwen
- `WHISPER_MODEL_URL`, `CLOUD_PUBLIC_URL` - URLs моделей Cloud.ru
- `AWS_S3_ENDPOINT_URL`, `AWS_STORAGE_BUCKET_NAME`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY` - настройки AWS S3

**Опциональные:**
- `DATABASE_URL` - Полный URL подключения к PostgreSQL (альтернатива отдельным переменным)
- `REDIS_URL` - Полный URL подключения к Redis (альтернатива отдельным переменным)
- `REDIS_PASSWORD` - Пароль Redis (если требуется)
- `DEBUG` - Режим отладки (True/False)
- `COMMUNITY_CHAT_URL` - URL чата сообщества
- `DJANGO_SUPERUSER_USERNAME`, `DJANGO_SUPERUSER_EMAIL`, `DJANGO_SUPERUSER_PASSWORD` - данные для создания суперпользователя

Полный список см. в `env.example`

## 🐳 Docker

- PostgreSQL 16 на порту 5432 (локально, если используется)
- Redis 7 на порту 6379 (локально, если используется)
- Django Admin на порту 8042
- Приложение использует внешние PostgreSQL и Redis из .env файла

## 📚 Технологии

- Python 3.12
- SQLAlchemy 2.0
- Alembic
- PostgreSQL 16
- Redis 7
- aiogram 3.13
- Pydantic v2
- Docker & Docker Compose


