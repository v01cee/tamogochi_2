# Core dependencies
# Build tools (нужны для установки некоторых пакетов)
setuptools>=68.0.0
wheel>=0.41.0

aiogram==3.13.1
limited-aiogram==1.0.0
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary>=2.9.10
redis==5.2.1
pydantic>=2.4.1,<2.10  # aiogram 3.13.1 требует pydantic<2.10
pydantic-settings==2.5.2
python-dotenv==1.0.1
APScheduler==3.10.4
robokassa==1.0.0
requests==2.32.3

# Admin panel
Django==5.1.2
django-jazzmin==3.0.1
django-storages>=1.14.0  # Для работы с S3 хранилищем
boto3>=1.34.0  # AWS SDK для Python (требуется для django-storages)

# Visualizations
numpy>=1.24.0,<2.0.0  # Для Python 3.11 есть готовые wheels
matplotlib==3.9.2  # Совместима с numpy 1.x

# Audio processing (для оптимизации аудио перед отправкой в Whisper)
pydub==0.25.1
# audioop встроен в стандартную библиотеку Python 3.11+


