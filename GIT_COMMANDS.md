# 📦 Команды для Git и деплоя

## Команды для работы с Git

### 1. Инициализация репозитория (если еще не инициализирован)

```bash
git init
```

### 2. Добавление всех файлов

```bash
git add .
```

### 3. Первый коммит

```bash
git commit -m "Initial commit: Telegram bot for Life Strategies course"
```

### 4. Добавление удаленного репозитория

```bash
# Если репозиторий на GitHub
git remote add origin https://github.com/ваш-username/название-репозитория.git

# Или если на другой платформе
git remote add origin https://gitlab.com/ваш-username/название-репозитория.git
```

### 5. Отправка в репозиторий

```bash
# Первая отправка
git push -u origin main

# Или если ветка называется master
git push -u origin master
```

### 6. Дальнейшие коммиты (после изменений)

```bash
# Добавить изменения
git add .

# Сделать коммит
git commit -m "Описание изменений"

# Отправить на сервер
git push
```

### 7. Проверка статуса

```bash
git status
```

### 8. Просмотр изменений

```bash
git diff
```

## Команды для деплоя на сервер

### Вариант 1: Клонирование с Git и запуск

```bash
# 1. Клонируем репозиторий на сервер
git clone https://github.com/ваш-username/название-репозитория.git
cd название-репозитория

# 2. Создаем .env файл
cp env.example .env
nano .env  # или vi .env - заполните все переменные

# 3. Запускаем деплой
chmod +x deploy.sh
./deploy.sh
```

### Вариант 2: Ручной запуск без скрипта

```bash
# 1. Клонируем репозиторий
git clone https://github.com/ваш-username/название-репозитория.git
cd название-репозитория

# 2. Создаем .env файл
cp env.example .env
nano .env  # заполните все переменные

# 3. Останавливаем старые контейнеры
docker-compose down

# 4. Собираем образы
docker-compose build --no-cache

# 5. Запускаем контейнеры
docker-compose up -d

# 6. Применяем миграции Alembic
docker-compose exec bot alembic upgrade head

# 7. Применяем миграции Django
docker-compose exec admin python admin_panel/manage.py migrate --noinput

# 8. Собираем статические файлы
docker-compose exec admin python admin_panel/manage.py collectstatic --noinput

# 9. Создаем суперпользователя (при первом запуске)
docker-compose exec admin python admin_panel/manage.py createsuperuser
```

### Вариант 3: Обновление существующего проекта

```bash
# 1. Переходим в директорию проекта
cd /path/to/project

# 2. Получаем последние изменения
git pull

# 3. Перезапускаем контейнеры
docker-compose down
docker-compose build --no-cache
docker-compose up -d

# 4. Применяем новые миграции (если есть)
docker-compose exec bot alembic upgrade head
docker-compose exec admin python admin_panel/manage.py migrate --noinput
```

## Проверка работы

```bash
# Статус контейнеров
docker-compose ps

# Логи бота
docker-compose logs -f bot

# Логи админки
docker-compose logs -f admin

# Все логи
docker-compose logs -f
```

## Полезные команды

```bash
# Остановка всех сервисов
docker-compose down

# Перезапуск бота
docker-compose restart bot

# Перезапуск всех сервисов
docker-compose restart

# Удаление всех данных (осторожно!)
docker-compose down -v
```

